name: Run SEDBuild on Windows

on:
  workflow_dispatch:

jobs:
  run-build:
    runs-on: [self-hosted, Windows, X64]

    steps:
      - name: Run run_build.ps1
        shell: powershell
        run: |
          & "C:\Users\pkummara\run_build.ps1"
